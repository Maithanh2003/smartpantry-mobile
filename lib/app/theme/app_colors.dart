import 'package:flutter/material.dart';

/// Figma login screen palette (dark + orange accent).
abstract final class AppColors {
  static const background = Color(0xFF0B0D10);
  static const surface = Color(0xFF15181E);
  static const surfaceElevated = Color(0xFF1C2028);
  static const border = Color(0xFF2A3038);
  static const primary = Color(0xFFFF6B2C);
  static const primaryPressed = Color(0xFFE85F24);
  static const textPrimary = Color(0xFFF5F5F5);
  static const textSecondary = Color(0xFF9CA3AF);
  static const textMuted = Color(0xFF6B7280);
  static const error = Color(0xFFEF4444);
}
